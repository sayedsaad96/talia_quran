# قائمة فحص Android الفعلي — Talia V1

**الحالة الحالية: `NOT RUN`.** تُملأ هذه القائمة فقط باستخدام Release Candidate موقع ومثبت من المسار الداخلي. لا يكفي Debug build لإغلاق G7 أو G9.

## هوية جلسة الاختبار

| الحقل | القيمة |
|---|---|
| المختبر | `PENDING` |
| التاريخ/الوقت | `PENDING` |
| Git commit | `PENDING — frozen clean commit required` |
| إصدار التطبيق/build | `PENDING` |
| Artifact SHA-256 | `PENDING` |
| رابط المسار الداخلي | `PENDING` |
| جهاز 1 / Android / المقاس | `PENDING` |
| جهاز 2 / Android / المقاس | `PENDING` |
| لغة التشغيل | العربية / English / كلاهما: `PENDING` |

## المتطلبات السابقة

- [ ] نسخة Release موقعة بهوية تطبيق نهائية — `NOT RUN`
- [ ] artifact مطابق للـ commit والبصمات في سجل الإصدار — `NOT RUN`
- [ ] تثبيت من Internal Track، لا من Debug/USB فقط — `NOT RUN`
- [ ] جهازان فعليان بإصدارَي Android أو عاملي شكل مختلفين — `NOT RUN`
- [ ] حساب اختبار ومستخدم ضيف واتصال يمكن قطعه واستعادته — `NOT RUN`

## تثبيت نظيف

### البدء والحساب

- [ ] التهيئة العربية والإنجليزية تظهر بلا قص أو اتجاه خاطئ — `NOT RUN`
- [ ] بدء الاستخدام كضيف — `NOT RUN`
- [ ] تسجيل الدخول، استعادة كلمة المرور، وتبديل الحساب — `NOT RUN`
- [ ] تسجيل الخروج مع مزامنة Bookmark معلقة يمنع فقد البيانات — `NOT RUN`
- [ ] المسار الإجباري يحتفظ بحالة قابلة للاستعادة ثم يستعيدها بعد الدخول — `NOT RUN`

### القرآن

- [ ] فتح المصحف والتنقل إلى أول صفحة وآخر صفحة — `NOT RUN`
- [ ] فحص عينات السور 1 و2 و9 و95 و97 بصريًا — `NOT RUN`
- [ ] عدم ظهور بسملة مضافة في التوبة، وصحة حدود الفاتحة وبقية السور — `NOT RUN`
- [ ] تطابق عرض QCF والـ fallback وعدم وجود نص فارغ — `NOT RUN`
- [ ] البحث والتفاصيل والإشارات المرجعية والاستئناف — `NOT RUN`
- [ ] تشغيل صوت متاح مخزن، ثم صوت يحتاج اتصالًا — `NOT RUN`
- [ ] المشاركة لا تقص أو تعيد صياغة نص القرآن — `NOT RUN`

### الأذكار

- [ ] وجهة الأذكار تبقى متاحة ولا تنهار مع allowlist صفرية — `NOT RUN`
- [ ] تظهر حالة فارغة آمنة بدل أي محتوى مرشح غير معتمد — `NOT RUN`
- [ ] لا تظهر فئات أو بطاقات أو أفعال إشعار لمحتوى غير معتمد — `NOT RUN`
- [ ] لا يظهر نص ديني مبتور أو fallback مكتوب يدويًا في الإشعارات — `NOT RUN`

### الحفظ Adult/Kids والخصوصية

- [ ] جلسة Adult باستخدام الميكروفون عند توفره — `NOT RUN`
- [ ] جلسة Kids باستخدام الميكروفون عند توفره — `NOT RUN`
- [ ] رفض إذن الميكروفون يتيح التقييم اليدوي في Adult — `NOT RUN`
- [ ] رفض إذن الميكروفون يتيح التقييم اليدوي في Kids — `NOT RUN`
- [ ] عدم توفر STT/no-speech/error يتيح المسار اليدوي ولا يمنح نتيجة تلقائية — `NOT RUN`
- [ ] وضع الطيران مع cache فارغ يسمح بالتقدم دون اشتراط نجاح الصوت البعيد — `NOT RUN`
- [ ] لا تتكرر النقاط أو XP أو streak أو review عند النقر/العودة المتكرر — `NOT RUN`
- [ ] رابط الخصوصية يعمل من الواجهة، والنص العربي والإنجليزي يصفان مزود التعرف بدقة — `NOT RUN`
- [ ] زر الخيار اليدوي في سياسة الخصوصية يفتح مسار الحفظ الحقيقي — `NOT RUN`

### الإشعارات والاستقرار

- [ ] الإشعارات العامة محلية اللغة ولا تحتوي نصًا دينيًا غير معتمد أو مقتطعًا — `NOT RUN`
- [ ] الخلفية/الاستئناف لا يكرران الجوائز أو الجلسة — `NOT RUN`
- [ ] لا crash أو شاشة فارغة أو نص قرآن مفقود أو خلط بين حسابين — `NOT RUN`
- [ ] مراجعة Android vitals/logcat للأخطاء الحرجة خلال الجلسة — `NOT RUN`

## مسار الترقية

- [ ] تثبيت آخر نسخة موزعة قبل V1 وإنشاء بيانات واقعية — `NOT RUN`
- [ ] الترقية إلى RC من دون مسح البيانات — `NOT RUN`
- [ ] بقاء Bookmarks والحفظ والخطط وXP/streak وKids والإعدادات — `NOT RUN`
- [ ] بقاء عزل مالكي البيانات بعد تسجيل الخروج/الدخول بحساب آخر — `NOT RUN`
- [ ] تحميل أصول القرآن/المحتوى المصححة دون حلقة ترحيل أو cache قديم — `NOT RUN`
- [ ] إعادة فحص المسار اليدوي والخصوصية والأذكار بعد الترقية — `NOT RUN`

## النتيجة

| القرار | الاختيار |
|---|---|
| Clean-install | `PENDING: PASS / FAIL` |
| Upgrade | `PENDING: PASS / FAIL` |
| G7 | `NOT RUN` |
| العيوب مع روابطها | `PENDING` |
| توقيع QA/التاريخ | `PENDING` |

أي فشل أو اختلاف في artifact يترك G7/G9 في `NO-GO` حتى الإصلاح وإعادة الاختبار.
